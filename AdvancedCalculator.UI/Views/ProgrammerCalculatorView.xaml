<UserControl x:Class="AdvancedCalculator.UI.Views.ProgrammerCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="480">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Display -->
            <RowDefinition Height="Auto" />  <!-- 4-Base Representation Box -->
            <RowDefinition Height="Auto" />  <!-- Word Size / Bit Toolbar -->
            <RowDefinition Height="*" />     <!-- Programmer Keypad -->
        </Grid.RowDefinitions>

        <!-- Main Display -->
        <Border Grid.Row="0" Background="{DynamicResource CardBrush}" CornerRadius="12" Padding="16" Margin="0,0,0,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource AccentSecondaryBrush}" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4" HorizontalAlignment="Right" />
                <TextBox Text="{Binding CurrentInput, Mode=TwoWay}" Foreground="{DynamicResource TextBrush}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Stretch" TextAlignment="Right" Background="Transparent" BorderThickness="0" IsReadOnly="True" Focusable="False" />
            </StackPanel>
        </Border>

        <!-- Base Representations (HEX, DEC, OCT, BIN) -->
        <Border Grid.Row="1" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="12" Margin="0,0,0,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel>
                <Grid Margin="0,2" Cursor="Hand">
                    <Grid.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SwitchBaseCommand}" CommandParameter="HEX" />
                    </Grid.InputBindings>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="HEX" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock Grid.Column="1" Text="{Binding HexDisplay}" FontFamily="{DynamicResource MonospaceFont}" Foreground="{DynamicResource TextBrush}" TextAlignment="Right" />
                </Grid>

                <Grid Margin="0,2" Cursor="Hand">
                    <Grid.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SwitchBaseCommand}" CommandParameter="DEC" />
                    </Grid.InputBindings>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="DEC" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock Grid.Column="1" Text="{Binding DecDisplay}" FontFamily="{DynamicResource MonospaceFont}" Foreground="{DynamicResource TextBrush}" TextAlignment="Right" />
                </Grid>

                <Grid Margin="0,2" Cursor="Hand">
                    <Grid.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SwitchBaseCommand}" CommandParameter="OCT" />
                    </Grid.InputBindings>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="OCT" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock Grid.Column="1" Text="{Binding OctDisplay}" FontFamily="{DynamicResource MonospaceFont}" Foreground="{DynamicResource TextBrush}" TextAlignment="Right" />
                </Grid>

                <Grid Margin="0,2" Cursor="Hand">
                    <Grid.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SwitchBaseCommand}" CommandParameter="BIN" />
                    </Grid.InputBindings>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="BIN" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" />
                    <TextBlock Grid.Column="1" Text="{Binding BinDisplay}" FontFamily="{DynamicResource MonospaceFont}" Foreground="{DynamicResource TextBrush}" TextAlignment="Right" TextWrapping="Wrap" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Bit Size Selector -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="QWORD (64)" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding SwitchBitSizeCommand}" CommandParameter="QWORD" />
            <Button Grid.Column="1" Content="DWORD (32)" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding SwitchBitSizeCommand}" CommandParameter="DWORD" />
            <Button Grid.Column="2" Content="WORD (16)" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding SwitchBitSizeCommand}" CommandParameter="WORD" />
            <Button Grid.Column="3" Content="BYTE (8)" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding SwitchBitSizeCommand}" CommandParameter="BYTE" />
        </Grid>

        <!-- Programmer Keypad (6 rows x 6 columns) -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Row 0: Lsh, Rsh, AND, OR, XOR, NOT -->
            <Button Grid.Row="0" Grid.Column="0" Content="Lsh" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding ShiftLeftCommand}" />
            <Button Grid.Row="0" Grid.Column="1" Content="Rsh" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding ShiftRightCommand}" />
            <Button Grid.Row="0" Grid.Column="2" Content="AND" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding OperatorCommand}" CommandParameter="AND" />
            <Button Grid.Row="0" Grid.Column="3" Content="OR" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding OperatorCommand}" CommandParameter="OR" />
            <Button Grid.Row="0" Grid.Column="4" Content="XOR" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding OperatorCommand}" CommandParameter="XOR" />
            <Button Grid.Row="0" Grid.Column="5" Content="NOT" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding UnaryOpCommand}" CommandParameter="NOT" />

            <!-- Row 1: NAND, NOR, A, B, C, C -->
            <Button Grid.Row="1" Grid.Column="0" Content="NAND" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding OperatorCommand}" CommandParameter="NAND" />
            <Button Grid.Row="1" Grid.Column="1" Content="NOR" Style="{DynamicResource SciFuncButtonStyle}" Command="{Binding OperatorCommand}" CommandParameter="NOR" />
            <Button Grid.Row="1" Grid.Column="2" Content="A" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="A" />
            <Button Grid.Row="1" Grid.Column="3" Content="B" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="B" />
            <Button Grid.Row="1" Grid.Column="4" Content="C" Style="{DynamicResource OperatorButtonStyle}" Command="{Binding ClearCommand}" />
            <Button Grid.Row="1" Grid.Column="5" Content="⌫" Style="{DynamicResource OperatorButtonStyle}" Command="{Binding BackspaceCommand}" />

            <!-- Row 2: D, E, F, 7, 8, 9 -->
            <Button Grid.Row="2" Grid.Column="0" Content="D" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="D" />
            <Button Grid.Row="2" Grid.Column="1" Content="E" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="E" />
            <Button Grid.Row="2" Grid.Column="2" Content="F" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="F" />
            <Button Grid.Row="2" Grid.Column="3" Content="7" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="7" />
            <Button Grid.Row="2" Grid.Column="4" Content="8" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="8" />
            <Button Grid.Row="2" Grid.Column="5" Content="9" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="9" />

            <!-- Row 3: 4, 5, 6, 1, 2, 3 -->
            <Button Grid.Row="3" Grid.Column="0" Content="4" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="4" />
            <Button Grid.Row="3" Grid.Column="1" Content="5" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="5" />
            <Button Grid.Row="3" Grid.Column="2" Content="6" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="6" />
            <Button Grid.Row="3" Grid.Column="3" Content="1" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="1" />
            <Button Grid.Row="3" Grid.Column="4" Content="2" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="2" />
            <Button Grid.Row="3" Grid.Column="5" Content="3" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="3" />

            <!-- Row 4: 0, ±, = -->
            <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Content="0" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendCharCommand}" CommandParameter="0" />
            <Button Grid.Row="4" Grid.Column="2" Content="±" Style="{DynamicResource NumberButtonStyle}" Command="{Binding UnaryOpCommand}" CommandParameter="NEG" />
            <Button Grid.Row="4" Grid.Column="3" Grid.ColumnSpan="3" Content="=" Style="{DynamicResource AccentButtonStyle}" Command="{Binding CalculateCommand}" />
        </Grid>
    </Grid>
</UserControl>
