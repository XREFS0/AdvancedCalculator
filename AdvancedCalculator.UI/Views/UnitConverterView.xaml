<UserControl x:Class="AdvancedCalculator.UI.Views.UnitConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="450">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Category Selector -->
            <RowDefinition Height="Auto" />  <!-- From / To Conversion Cards -->
            <RowDefinition Height="Auto" />  <!-- Formula / Helper Text -->
            <RowDefinition Height="*" />     <!-- Number Pad -->
        </Grid.RowDefinitions>

        <!-- Category Combo -->
        <ComboBox Grid.Row="0" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" Style="{DynamicResource ModernComboBox}" Margin="0,0,0,16" />

        <!-- Converter Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- From Unit Box -->
            <Border Grid.Row="0" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="12" Margin="0,0,0,8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding FromValue}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" />
                        <ComboBox ItemsSource="{Binding AvailableUnits}" SelectedItem="{Binding FromUnit, Mode=TwoWay}" DisplayMemberPath="Name" Style="{DynamicResource ModernComboBox}" Margin="0,8,0,0" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="⇄" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding SwapUnitsCommand}" FontSize="18" Width="40" Height="40" Margin="8,0,0,0" ToolTip="Swap units" />
                </Grid>
            </Border>

            <!-- To Unit Box -->
            <Border Grid.Row="1" Background="{DynamicResource CardBrush}" CornerRadius="10" Padding="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding ToValue}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" TextTrimming="CharacterEllipsis" />
                    <ComboBox ItemsSource="{Binding AvailableUnits}" SelectedItem="{Binding ToUnit, Mode=TwoWay}" DisplayMemberPath="Name" Style="{DynamicResource ModernComboBox}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Formula Text -->
        <TextBlock Grid.Row="2" Text="{Binding FormulaText}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" Margin="4,0,0,12" />

        <!-- Keypad (4 rows x 3 columns) -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Content="7" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="7" />
            <Button Grid.Row="0" Grid.Column="1" Content="8" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="8" />
            <Button Grid.Row="0" Grid.Column="2" Content="9" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="9" />

            <Button Grid.Row="1" Grid.Column="0" Content="4" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="4" />
            <Button Grid.Row="1" Grid.Column="1" Content="5" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="5" />
            <Button Grid.Row="1" Grid.Column="2" Content="6" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="6" />

            <Button Grid.Row="2" Grid.Column="0" Content="1" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="1" />
            <Button Grid.Row="2" Grid.Column="1" Content="2" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="2" />
            <Button Grid.Row="2" Grid.Column="2" Content="3" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="3" />

            <Button Grid.Row="3" Grid.Column="0" Content="." Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="." />
            <Button Grid.Row="3" Grid.Column="1" Content="0" Style="{DynamicResource NumberButtonStyle}" Command="{Binding AppendDigitCommand}" CommandParameter="0" />
            <Button Grid.Row="3" Grid.Column="2" Content="⌫" Style="{DynamicResource OperatorButtonStyle}" Command="{Binding BackspaceCommand}" />
        </Grid>
    </Grid>
</UserControl>
