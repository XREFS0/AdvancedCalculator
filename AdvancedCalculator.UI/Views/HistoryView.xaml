<UserControl x:Class="AdvancedCalculator.UI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="500">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Header / Search / Actions -->
            <RowDefinition Height="*" />     <!-- List of calculations -->
            <RowDefinition Height="Auto" />  <!-- Footer buttons -->
        </Grid.RowDefinitions>

        <!-- Search Bar and Action Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource ModernTextBox}" Margin="0,0,8,0" Tag="Search history..." />
            <Button Grid.Column="1" Content="Clear All" Style="{DynamicResource CalcButtonBase}" Command="{Binding ClearAllCommand}" Height="38" Padding="12,0" Margin="0,0,6,0" />
            <Button Grid.Column="2" Content="Export" Style="{DynamicResource CalcButtonBase}" Command="{Binding ExportCsvCommand}" Height="38" Padding="12,0" />
        </Grid>

        <!-- Records List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Records}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBrush}" CornerRadius="8" Padding="14,10" Margin="0,0,0,8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Expression}" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="14" />
                                    <TextBlock Text="{Binding Result}" Foreground="{DynamicResource TextBrush}" FontSize="20" FontWeight="Bold" Margin="0,4,0,0" />
                                    <TextBlock Text="{Binding CreatedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="★" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Foreground="{DynamicResource WarningBrush}" FontSize="16" Width="32" Height="32" Margin="2,0" />
                                    <Button Content="📋" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding DataContext.CopyResultCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" FontSize="14" Width="32" Height="32" Margin="2,0" />
                                    <Button Content="🗑" Style="{DynamicResource MemoryButtonStyle}" Command="{Binding DataContext.DeleteRecordCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Foreground="{DynamicResource ErrorBrush}" FontSize="14" Width="32" Height="32" Margin="2,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,8,0,0">
            <TextBlock Text="Tip: Star favorite calculations to pin them at top." Foreground="{DynamicResource TextMutedBrush}" FontSize="12" />
            <TextBlock Text="{Binding ExportStatus}" Foreground="{DynamicResource AccentSecondaryBrush}" FontSize="12" FontWeight="SemiBold" Margin="16,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
